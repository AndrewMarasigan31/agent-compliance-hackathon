# Ralph Progress Log
Started: Sat May  9 13:29:50 PST 2026
---

## 2026-05-09 - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented nearestNeighborTSP in beat-planner/lib/routing.ts
- Files changed: beat-planner/lib/routing.ts (new file)
- **Learnings for future iterations:**
  - lib/ directory is for shared utility modules (see lib/clustering.ts pattern)
  - Typecheck is `npx tsc --noEmit` from beat-planner directory
  - Matrix indexing: matrix[current][j] gives travel cost from current to j
---

## 2026-05-09 - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented POST /api/route endpoint in beat-planner/app/api/route/route.ts
- Computes centroid, finds nearest store to centroid as start, calls OSRM Table API, applies nearestNeighborTSP, returns orderedStores
- Files changed: beat-planner/app/api/route/route.ts (new file)
- **Learnings for future iterations:**
  - Next.js API routes live at app/api/<name>/route.ts; file exports named HTTP method functions (POST, GET, etc.)
  - OSRM Table API format: coords are lng,lat (longitude first), semicolon-separated
  - OSRM response has a `durations` 2D array — pass directly as the distance matrix to nearestNeighborTSP
  - Use AbortController with setTimeout for fetch timeouts (no built-in signal timeout in Node fetch)
---

## 2026-05-09 - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added Beat Routes panel alongside the Leaflet map in BeatMap.tsx
- Panel is a scrollable w-72 sidebar showing each beat: colored swatch, Beat ID, GCU, store count, route status badge, Generate Route button
- Added RouteStatus type ("not-routed" | "loading" | "routed" | "error") and routeStatuses state (Record<number, RouteStatus>)
- Route status badge shows gray/blue/green/red for each state; button shows spinner and is disabled during loading
- useEffect clears stale routeStatuses when beats array changes (handles lasso reassignment removing beats)
- handleGenerateRoute stub sets status to "loading"; US-004 will complete the API call
- Map wrapped in flex layout (flex-1) alongside panel (w-72 flex-shrink-0)
- Files changed: beat-planner/components/BeatMap.tsx
- **Learnings for future iterations:**
  - routeStatuses and handleGenerateRoute live in BeatMap — US-004 should extend handleGenerateRoute there (not in dashboard/page.tsx)
  - The panel uses `beats` prop directly (not visibleBeats/filtered) so all beats always show regardless of GCU filter
  - dev-browser skill was not available; browser verification skipped — typecheck passed
  - BeatMap had substantial uncommitted changes from prior sessions (lasso tool, etc.) — committed together with US-003
---

## 2026-05-09 - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Completed handleGenerateRoute in BeatMap.tsx: calls POST /api/route, stores orderedStores in routedStores state, updates routeStatus to "routed" or "error"
- Updated marker redraw useEffect: routed beats draw numbered DivIcon markers + Leaflet Polyline; unrouted beats keep default circle markers; effect now depends on routedStores
- Added routedStores state (Record<number, BeatStore[]>) cleared alongside routeStatuses when beats change
- Updated error badge text to "Route failed — try again"
- Files changed: beat-planner/components/BeatMap.tsx
- **Learnings for future iterations:**
  - routedStores is internal BeatMap state; the marker redraw effect clears all non-tile layers and redraws from state on every change — no need to track layer references per beat
  - DivIcon markers use inline HTML div with flexbox for numbered circles; className must be "" to avoid Leaflet's default icon styling
  - The dashboard is at /dashboard (redirects to /login if unauthenticated); dev-browser skill not available, browser verification skipped — typecheck confirmed passing
---

## 2026-05-09 - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added route invalidation in handleReassign: clears routeStatuses and routedStores for all source beats + target beat before calling onBeatsChange
- Both affected beats revert to "Not routed" status and markers/polylines are removed by the existing marker redraw useEffect
- Files changed: beat-planner/components/BeatMap.tsx
- **Learnings for future iterations:**
  - Spread Set into array before iterating to avoid TS2802 (downlevelIteration) error; tsconfig targets older ES
  - The existing beats-change useEffect only removes entries for deleted beats; active beats with changed stores need explicit invalidation at the reassign call site
---
